type str = string;
type responseType = {
    status: number;
    OldFolderDeleted: boolean;
    message: string;
};
export default function checkFileExists(FolderPATH: str, AutoDelete?: boolean): Promise<responseType>;
export {};

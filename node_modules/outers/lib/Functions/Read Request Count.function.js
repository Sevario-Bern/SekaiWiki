"use strict";
var __awaiter = (this && this.__awaiter) || function (thisArg, _arguments, P, generator) {
    function adopt(value) { return value instanceof P ? value : new P(function (resolve) { resolve(value); }); }
    return new (P || (P = Promise))(function (resolve, reject) {
        function fulfilled(value) { try { step(generator.next(value)); } catch (e) { reject(e); } }
        function rejected(value) { try { step(generator["throw"](value)); } catch (e) { reject(e); } }
        function step(result) { result.done ? resolve(result.value) : adopt(result.value).then(fulfilled, rejected); }
        step((generator = generator.apply(thisArg, _arguments || [])).next());
    });
};
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
/* eslint-disable @typescript-eslint/no-explicit-any */
const ShortStorage_storage_1 = __importDefault(require("../Storage Management/ShortStorage.storage")); // Importing ShortStorage
const Middleware_Constant_1 = require("../Config/Constant/Middleware.Constant"); // Importing Today's Date
// Main Function
/**
 * Retrieves the request count from the storage.
 * If a RequestDate is provided, retrieves the request count for that specific date.
 * If no RequestDate is provided, retrieves the overall request count.
 * @param {string} RequestDate - The date for which to retrieve the request count. If not provided, retrieves the overall request count.
 * @param {string} RequestDate - The Date Must Be M/D/YYYY format. like : 3/8/2024
 * @returns {Promise<number>} - A promise that resolves to the request count.
 * @throws {Error} - If unable to read the request count from the storage.
 */
function default_1(RequestDate) {
    return __awaiter(this, void 0, void 0, function* () {
        try {
            const StorageInstance = new ShortStorage_storage_1.default(Middleware_Constant_1.RequestLoggerCredentials.RequestLoggerStorageName, Middleware_Constant_1.RequestLoggerCredentials.RequestLoggerStorageDefaultSize, Middleware_Constant_1.RequestLoggerCredentials.RequestLoggerStorageEncryptionKey); // Create new ShortStorage instance
            if (RequestDate === undefined ||
                RequestDate === null ||
                RequestDate === "") {
                return yield StorageInstance.Get(); // Get All Data in Storage File
            }
            else {
                return yield StorageInstance.Get(RequestDate);
            }
        }
        catch (error) {
            throw new Error("Unable to read the request count from the storage.");
        }
    });
}
exports.default = default_1;
//# sourceMappingURL=Read%20Request%20Count.function.js.map